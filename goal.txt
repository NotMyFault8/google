1. 扩大训练集：把你的训练样本从 200k 提升到 N（可控内存）
建议你做一个“增长实验表”：

200k / 500k / 1M

每次记录 Ridge_log / HGB_log 的 RMSE

这样你的报告会很硬：“数据量增加 → 模型性能变化曲线”。


2. HistGradientBoosting + 5-fold CV
    线性大数据版本（可扩到百万+）：SGDRegressor（可选）

当你数据特别大，且你想要一个“可解释 + 训练快”的线性模型